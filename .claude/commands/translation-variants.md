Read `4-SYSTEM/Skills/translation-variants/SKILL.md` in full, then execute it on the file(s) or input the user specifies.

Skill purpose: Extract every translation variant for each source-language term across a pair of aligned source and target files, and write a YAML frequency table (pali_stem → en_rendering: count).
